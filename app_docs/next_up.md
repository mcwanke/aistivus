

# button update on evaluate
- on the "Fill With AI" button on evalute can we add a small spinner next to the button to show that the processing is ongoing?

# basic search field on jobs page
- on the jobs page can we add a simple search textbox to allow the user to type in text that will filter the results from the company name and title? This should be in addition to the other filters

# on e recent evaluation I forgot to copy in the job title
- I don't see where I can edit the job title after creation - need to add that into the app. Probably into the Job Details Summary -> JOB INFO section

# just realized that if all AI instances are down there is no way to import a resume
- Need to add a straight import function in evaluate that skips local/AI evaluatino and simply imports the job information so that it enters into the system