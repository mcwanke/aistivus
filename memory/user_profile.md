---
name: user-profile
description: User's role, goals, and key constraints for this project
metadata:
  type: user
---

Primary developer of AIstivus. Technical, but has non-technical friends who will use the app. This means the Settings UI must remain the primary way to change app settings — no "just edit a config file" shortcuts are acceptable for features non-technical users need to access.

Prefers SemVer over CalVer (confirmed 2026-05-15).

Works iteratively — reviews plans before implementation, makes some edits manually, delegates others. Values tight planning discussions before code is written.
