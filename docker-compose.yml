services:
  aistivus:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./user_data:/app/user_data
      - ./app_data:/app/app_data
    env_file:
      - .env
    restart: unless-stopped
